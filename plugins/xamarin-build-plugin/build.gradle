version = '0.1.1-iHomeAutomate'
ext.artifactId = 'xamarin-build-plugin'

task sourceJar(type: Jar) {
    from sourceSets.main.allSource
}

publishing {
    publications {
        mavenStuff(MavenPublication) {
            from components.java

            artifact sourceJar {
                classifier "sources"
            }
        }
    }
}